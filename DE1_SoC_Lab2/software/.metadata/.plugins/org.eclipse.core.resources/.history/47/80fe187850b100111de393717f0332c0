.text

# define a macro to move a 32 bit address to a register

.macro MOVIA reg, addr
  movhi \reg, %hi(\addr)
  ori \reg, \reg, %lo(\addr)
.endm


# def hex 0
.equ HEX0, 0x11010
.equ ZEROHEX, 0x3F
.global main

main:
 movia r2, HEX0
 movia r3, ZEROHEX

loop:
 stbio r3, 0(r2)
    br loop
